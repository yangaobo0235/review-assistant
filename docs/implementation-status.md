# 架构改造执行状态

当前正式业务仅为青岛报废置换和长春报废置换。过户规则已从生产注册表、规则分发和前端业务识别中移除；兼容名称只用于历史测试和数据迁移，不会被生产 Profile 解析。

已落地：

- 通用 LangGraph 主图新增能力规划节点；
- 材料提取与字段比较解耦，比较阶段不默认重复 OCR/Vision；
- 新增统一字段规格目录；
- 新增 TypeScript 页面动作协议和页面动作控制器；
- 回填后支持定位和高亮，不自动改变人工处理状态；
- 前端构建、TypeScript 类型检查和 ESLint 通过。

仍需后续迁移：

- 将历史测试中的过户场景改写为当前报废置换契约；
- 将 public 下的页面脚本完整迁移为 TypeScript 多入口；
- 将 EvidenceFact、CheckResult、ReviewTask 统一为稳定后端协议；
- 把页面采集器、控件解析器和挂靠动作进一步拆成适配器模块；
- 使用真实 Chrome 页面完成回填、下拉选择、回读、定位和高亮验收。
