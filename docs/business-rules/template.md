# {地区或业务名称}审核规则（`{business_type} | {region} | {version}`）

本文档记录一个可上线审核 Profile 的完整业务契约。规则实现、前端行为、测试和文档必须以同一个 Profile ID 和版本关联。

## 1. 业务身份

| 项目 | 配置 |
| --- | --- |
| 业务类型 | |
| 地区/租户 | |
| Profile ID | |
| 版本 | |
| 页面路径和指纹 | |
| 启用状态 | `active` / `disabled` / `unconfigured` |
| 自动建议范围 | |
| 人工复核入口 | |

说明页面如何识别、哪些条件会阻止审核、停用或未配置时用户看到什么。

## 2. 前端规则

记录 Adapter 的页面探测、字段映射、材料分组、工作台 Renderer、可见标签、任务排序、人工操作、PageAction ID、允许写入字段、原值校验、回读和回滚。明确哪些内容只是展示格式，哪些内容必须原样显示后端状态。

## 3. 后端字段和材料

列出所有必审字段、规范字段路径、数据来源、是否允许 OCR、是否允许人工录入、归一化规则、证据优先级和缺失语义。列出材料类型、业务范围、最小数量、必需页码、读取失败和识别失败的处理方式。

## 4. 能力和规则

| 能力 ID | 类型 | 阶段 | 输入事实 | 输出事实 | 依赖 | 超时/重试 | 失败策略 |
| --- | --- | --- | --- | --- | --- | --- | --- |

逐项说明算法、证据来源、`MATCH`、`CONFLICT`、`INSUFFICIENT`、`REVIEW_REQUIRED` 的触发条件，以及是否允许产生页面动作。

## 5. 业务例外

记录边界日期、特殊材料、同主体/跨主体关系、外部服务不可用、重复材料、冲突证据、页面刷新、重复提交和权限拒绝时的处理。任何无法安全自动判断的情况必须进入人工复核并说明原因。

## 6. 注册和代码位置

列出 Profile、CapabilitySpec、Binding、Rule Registry、External Check Registry、PageAdapter、PageAction 和 Renderer 的代码位置，以及对应测试文件。新能力必须通过注册表接入，禁止在主图节点、API 路由或 React 组件中硬编码业务分支。

## 7. 变更记录和验收

记录规则版本、变更日期、变更原因、影响范围、兼容策略和回滚方式。验收至少覆盖成功、缺失、冲突、不确定、超时、重复请求、页面失效和权限拒绝场景，并附上实际验证命令。

## 8. 文档维护边界

本业务文档记录该 Profile 的“实际配置和实际表现”，包括注册表条目与前端展示；`docs/registries-and-subgraphs.md` 记录所有 Profile 共用的注册机制、子图生命周期和技术约束；`docs/frontend-presentation.md` 记录通用颜色、状态、定位和回填交互。三类文档分别回答“这个业务配置了什么”“系统如何接入能力”“审核员在页面上看到什么”，不要互相复制完整内容。
