# 安全策略

## 报告安全问题

请优先使用 GitHub 仓库的私有漏洞报告功能，或通过仓库维护者认可的私下渠道报告。不要在普通 Issue、讨论区或 Pull Request 中公开漏洞细节、凭据或真实业务数据。

报告中请包含受影响模块、复现条件、影响范围和建议的缓解方式，但不要附带真实证件、车辆材料或生产密钥。

## 密钥管理

- DashScope API Key 只能保存在本地 `.env` 或部署平台的秘密管理服务中。
- `.env.example` 只保留不可用的占位值。
- 一旦密钥出现在提交、日志、截图或聊天记录中，应立即撤销并重新签发；仅删除文件不足以消除泄露风险。
- CI 不配置真实模型密钥，自动化测试不得访问真实 DashScope 服务。

## 业务数据

车辆材料、证件、页面字段和审核结果可能包含敏感信息。开发与测试应使用脱敏或合成数据，并限制本地文件、日志和调试输出的保留时间与访问范围。

## 部署边界

当前实现面向受控本地开发和验证，不具备直接暴露公网所需的认证授权、租户隔离、持久化任务队列、全局限流、完整审计和数据生命周期管理。生产部署前必须补齐这些能力，并收紧浏览器扩展的页面匹配范围和服务地址权限。
